{% extends 'base.twig' %}
{% block content %}
    <div class="container">
        <div class="content">
            <h1 class="section-title">{{ title }}</h1>
            <p>{{ content }}</p>
        </div>
    </div>
{% endblock %}
